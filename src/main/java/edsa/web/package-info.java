/**
 * The five pages: controllers and Thymeleaf views.
 *
 * <p>May depend on {@code edsa.data} and {@code edsa.core}. Pages never decide anything —
 * a controller takes the request, asks the core, and shows the answer. Any allocation or
 * rule logic written here is in the wrong package.
 */
package edsa.web;
