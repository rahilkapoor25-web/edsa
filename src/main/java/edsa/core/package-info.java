/**
 * Domain model, constraints and solver: everything that decides.
 *
 * <p>Depends on nothing else in the project. Must never import {@code edsa.web},
 * {@code edsa.data}, Spring, or any web or persistence framework — plain Java only,
 * so the decision-making can be tested and run headlessly without a browser.
 */
package edsa.core;
